<%= render "shared/project", admin: true %>
<%= render "shared/lcar",
  elements: @projects,
  element_height: @element_height,
  sm_count:  @sm_count,
  md_count: @md_count,
  lg_count: @lg_count,
  xl_count: @xl_count,
  ms_per_scroll: @ms_per_scroll,
  full: true
%>
