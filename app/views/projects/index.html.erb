<%= render "shared/banner", title: "Projects", details: "We are an established, innovative energy and sustainability consultancy that provide a full suite of modelling and compliance solutions across all sectors of the property and construction industry." %>

<div class="projects-grid">
  <% @projects.each do |project| %>
    <%= link_to project_path(project) do  %>
      <%= render "shared/project_card", project: project %>
    <% end %>
  <% end %>
</div>

<%= render 'shared/partners_and_accreditations' %>
