<% if link_path %>
  <%= link_to link_path,
    class: "element",
    style: "
      height: #{height};
      width: #{(1 / sm_count) * 100}%;
      background-color: #{background_color};
    " do  %>
    <div class="project-card-categories">
      <% element.services.each do |service| %>
        <%= image_tag service.photo, alt: "alttext", class: "service-svg-small" %>
      <% end %>
    </div>
    <h4><%= element.title %></h4>
    <h6><%= element.services_sentence %></h6>
  <% end %>
<% else %>
  <div
    class="element"
    style="
      height: <%= height %>;
      width: <%= (1 / sm_count) * 100 %>%;
      background-color: <%= background_color %>;
    ">
    <div class="project-card-categories">
      <% element.services.each do |service| %>
        <%= image_tag service.photo, alt: "alttext", class: "service-svg-small" %>
      <% end %>
    </div>
    <h4><%= element.title %></h4>
    <h6><%= element.services_sentence %></h6>
  </div>
<% end %>

